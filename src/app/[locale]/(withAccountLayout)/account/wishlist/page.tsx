import PagePlaceholder from "@/components/shared/PagePlaceholder"

export default function WishlistPage() {
	return <PagePlaceholder title="Wishlist" note="The wishlist is not built yet. The API already merges a guest list on sign-in." />
}
