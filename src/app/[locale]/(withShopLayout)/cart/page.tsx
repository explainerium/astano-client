import PagePlaceholder from "@/components/shared/PagePlaceholder"

export default function CartPage() {
	return <PagePlaceholder title="Cart" note="The cart is not built yet. The API supports guest carts and live pricing." />
}
